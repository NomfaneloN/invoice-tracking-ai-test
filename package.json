{
  "name": "invoice-tracking-ai-test",
  "version": "1.0.0",
  "description": "Markdown-driven E2E tests for ITS Shesha3 — Claude drives the browser via MCP tools",
  "scripts": {
    "allure:results": "node scripts/generate-allure-results.js",
    "allure:build": "allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report",
    "allure:report": "npm run allure:results && npm run allure:build && npm run allure:open"
  },
  "devDependencies": {
    "allure-commandline": "^2.30.0"
  }
}
